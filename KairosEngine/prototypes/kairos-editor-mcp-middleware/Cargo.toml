[workspace]

[package]
name = "kairos-editor-mcp-middleware-prototype"
version = "0.1.0"
edition = "2024"
description = "Prototype: middleware architecture for kairos_editor_mcp → KairosEngine communication"

[dependencies]
tokio = { version = "1", features = ["sync", "macros", "rt"] }
serde = { version = "1", features = ["derive"] }

/target
# The asset processor's write-ahead log (the processed assets under
# `imported_assets/Default` are committed; regenerate them from the project
# sources with `cargo run --bin bake_assets`).
/imported_assets/log

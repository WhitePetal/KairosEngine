[package]
name = "kairos_ecs_macros"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Derive macros for the KairosEngine ECS (Component, Resource, ...)"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
kairos_macro_utils = { path = "../../macro_utils" }
